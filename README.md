I got the fighter stats and names from a random csv i downloaded from a UFC website. 

I got the images (Except the default PFP and Conor Mcgregor images) by wrighting a script that gets the wikipedia image adress of every fighter that has an image on wikipedia, by using a wikipedia API.

